Review Findings
- High: tests/integration/conftest.py:42 and tests/integration/conftest.py:56 run Base.metadata.drop_all(test_engine) against whatever DATABASE_URL points to. This satisfies fail-fast real DB testing, but it is dangerous if someone accidentally points DATABASE_URL at a dev or production database. Improvement: require database name to end with _test or add an explicit ALLOW_DESTRUCTIVE_TEST_DB=1 guard.
- Medium: src/gym_tracker/entrypoints/dependencies.py:13 leaves postgres_client as None, while src/gym_tracker/exercise_ingestor/transformer.py:48 still calls postgres_client.info.status. Running that ingestion script will now fail with AttributeError. Improvement: update the script to create its own connection/session or remove the stale runtime dependency in a follow-up.
- Medium: src/gym_tracker/adapters/repositories.py:101 uses with self.session.begin(). This works for the tested API path, but it can raise InvalidRequestError if add_workout is called after the same session has already implicitly opened a transaction. Improvement: define transaction ownership at the dependency/service boundary or use a session-per-operation pattern consistently.
- Low: static/js/add_workout.js:260 still logs the full create payload with console.log(payload). Improvement: remove this before shipping.
- Low: src/gym_tracker/adapters/mappers.py:83-99 still relies on dotted form keys and silently skips unknown attributes. Malformed input can still reach repository code as missing weight or repetitions and produce a 500. Improvement: replace this with an explicit Pydantic create schema later.